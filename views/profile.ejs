<!DOCTYPE html>
<html>
    <head>
        <style>
            body{background: skyblue; font-family: blippo; color: #fff; padding: 30px;}
            h1{font-size: 48px; text-transform: uppercase; letter-spacing: 2px; text-align: center;}
            p{font-size: 16px; text-align: center;}
        </style>
        <title> TOHOME </title>
    </head>
    <body>
        <h1>Hello admin.</h1>
        <ul>
            <% if(data.length != 0) data.forEach(v => { %>
            <li> <%=v%><li>
            <% }) %>
        </ul>
    </body>

</html>